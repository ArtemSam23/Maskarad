# Отдельный SA без токена: фильтр kubernetes работает с use_tag_for_meta и в API не ходит,
# ClusterRole не нужна.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: fluent-bit
automountServiceAccountToken: false
